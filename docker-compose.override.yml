services:
  ourtime_web:
    command: >
      bash -c "
      rm -f tmp/pids/server.pid &&
      /usr/local/bin/wait-for-it ourtime_db:5432 --timeout=30 --strict -- 
      bin/rails server -b 0.0.0.0 -p 3000
      "